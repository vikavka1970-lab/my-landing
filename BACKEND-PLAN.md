# BACKEND-PLAN.md — Архитектура бэкенда

Проект: лендинг + Telegram Mini App «Бриф-бот».  
Составлено: май 2026. Только то, что реально нужно прямо сейчас.

---

## Что есть без бэкенда (не трогать)

| Компонент | Как работает сейчас |
|---|---|
| Лендинг `index.html`, `pricing.html` | Статические файлы. Бэкенд не нужен |
| Форма на лендинге | Web3Forms (внешний сервис). Ключ `95a698d3-f7e8-4a29-874c-993817c7263a`. Письма уходят на `vikavka1970@gmail.com`. Бэкенд не нужен |
| Mini App `tg-app/index.html` | Статика на Vercel. Бэкенд не нужен |
| Offer modal | Состояние в `localStorage` ключ `vp_offer_seen`. Бэкенд не нужен |

**Итог:** весь существующий стек работает без сервера. Бэкенд нужен ровно для одного — принять бриф из Mini App и доставить его Виктории в Telegram.

---

## Единственный компонент, который нужно написать

### Telegram-бот (Node.js + Telegraf)

**Что делает:**
1. Принимает событие `web_app_data` от Mini App
2. Парсит JSON из `ctx.webAppData.data`
3. Форматирует и отправляет сообщение в личку Виктории
4. Обрабатывает команду `/start` (приветствие + кнопка открыть Mini App)
5. Обрабатывает `/start from_app` — когда пользователь нажал «Получить скидку 10%» в offer modal

**Что НЕ делает:**
- Не хранит данные в базе
- Не строит admin-панель
- Не ведёт аналитику
- Не управляет пользователями

---

## Данные

### Что Mini App отправляет боту

```json
{
  "name":       "Максим Иванов",
  "username":   "maxim_ivanov",
  "type":       "Сайт / лендинг",
  "timeline":   "Стандартно (1–2 месяца)",
  "budget":     "30 000 – 80 000 ₽",
  "priorities": "Дизайн и качество, Прозрачность и общение",
  "contact":    "+7 999 000 00 00",
  "details":    "нужен лендинг для кофейни, есть брендбук"
}
```

Поля `contact` и `details` — необязательные, могут быть пустыми строками.

### Что Виктория получает в Telegram

```
📋 Новая заявка из Mini App

👤 Имя: Максим Иванов (@maxim_ivanov)
🎯 Проект: Сайт / лендинг
⏰ Срок: Стандартно (1–2 месяца)
💰 Бюджет: 30 000 – 80 000 ₽
⭐ Важно: Дизайн и качество, Прозрачность и общение
📞 Контакт: +7 999 000 00 00
💬 Детали: нужен лендинг для кофейни, есть брендбук
```

Если поле пустое — строка не показывается.

---

## Переменные окружения

Бот требует ровно две переменные:

| Переменная | Значение | Где взять |
|---|---|---|
| `BOT_TOKEN` | `8615590215:AAGRCTOxNxz9kx-VA5b2XbLUSSXMyC8_BAY` | Уже есть в `.env` |
| `VICTORIA_CHAT_ID` | Числовой ID Telegram-аккаунта Виктории | Узнать через `@userinfobot` |

`.env` добавлен в `.gitignore`. В деплой (Railway) передаются через панель управления.

---

## Роли и доступ

| Роль | Кто | Что видит | Что делает |
|---|---|---|---|
| Клиент | Любой пользователь Telegram | Mini App — 7 экранов брифа | Заполняет бриф, нажимает «Отправить» |
| Виктория | `@viktoriapoltavchenko` | Сообщение в личку с брифом | Читает, отвечает клиенту в Telegram напрямую |
| Бот | `@Lendingferst_bot` | JSON из Mini App | Форматирует и пересылает. Больше ничего |

Никакой admin-панели нет. Интерфейс Виктории — её личный Telegram.

---

## Где деплоить бота

Бот должен работать **постоянно** (не serverless). Два варианта:

### Вариант A — Railway (рекомендуется для v1)

- Постоянно работающий Node.js процесс
- Бесплатный стартовый тариф ($5 кредит)
- Режим: polling (`bot.launch()`) — не нужен webhook
- Деплой: `git push` → Railway автоматически деплоит из GitHub

**Плюсы:** проще в настройке, не нужен webhook.  
**Минусы:** нужна банковская карта для регистрации (или GitHub Student).

### Вариант B — Vercel Serverless Function

- Добавить `tg-app/api/webhook.js` в существующий Vercel-проект
- Режим: webhook — Telegram шлёт POST на `https://vp-brief-bot.vercel.app/api/webhook`
- Уже есть инфраструктура, деплой автоматический

**Плюсы:** zero cost, уже настроен деплой.  
**Минусы:** cold start ~1 сек, нужно зарегистрировать webhook через Telegram API.

**Решение:** начать с Vercel (меньше новых сервисов). Перенести на Railway если появятся проблемы с cold start.

---

## Файловая структура (Vercel-вариант)

```
tg-app/
├── index.html          ← Mini App (уже готов)
├── vercel.json         ← роуты (обновить)
├── api/
│   └── webhook.js      ← Telegraf-бот как serverless function
└── .env                ← BOT_TOKEN, VICTORIA_CHAT_ID (gitignored)
```

Обновлённый `vercel.json`:
```json
{
  "outputDirectory": ".",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)",     "destination": "/index.html" }
  ]
}
```

---

## Команды бота

| Команда / событие | Что делает |
|---|---|
| `/start` | Отправляет приветствие + инлайн-кнопку «Заполнить бриф» → открывает Mini App |
| `/start from_app` | То же + текст «Ваш промокод −10% будет отправлен отдельно» (offer modal) |
| `web_app_data` | Парсит JSON брифа → форматирует → отправляет Виктории |

---

## Что НЕ строим в v1

| Что | Почему не нужно |
|---|---|
| База данных | Брифы идут прямо в Telegram. Виктория отвечает вручную |
| Admin-панель | Telegram — её интерфейс |
| Авторизация | Пользователи анонимны, бот публичный |
| Аналитика | Нет запроса, нет данных для анализа |
| Email-уведомления | Telegram быстрее, форма лендинга уже даёт email |
| Хранение истории диалогов | Telegram хранит сам |
| Rate limiting | Нагрузка минимальная (один фрилансер) |

---

## Следующий шаг — v2 (AI-слой)

Из `brief.md`, Фаза 2: заменить статичные шаги на диалог с Claude Haiku.

Тогда понадобится:
- **Сессионное состояние** — хранить контекст диалога пока пользователь проходит бриф (in-memory Map или Upstash Redis с TTL 30 минут)
- **Anthropic API** — Claude Haiku для уточняющих вопросов
- **Новая переменная окружения:** `ANTHROPIC_API_KEY`

Но это v2. В v1 не строим.

---

## Порядок реализации v1

1. Узнать `VICTORIA_CHAT_ID` через `@userinfobot`
2. Создать `tg-app/api/webhook.js` с Telegraf
3. Добавить `package.json` в `tg-app/` с зависимостью `telegraf`
4. Прописать `BOT_TOKEN` и `VICTORIA_CHAT_ID` в Vercel → Settings → Environment Variables
5. Задеплоить (автоматически при `git push`)
6. Зарегистрировать webhook: `GET https://api.telegram.org/bot<TOKEN>/setWebhook?url=https://vp-brief-bot.vercel.app/api/webhook`
7. Протестировать: пройти бриф в Telegram → убедиться что сообщение пришло
